# @substrate/runtime-darwin-arm64

Prebuilt Substrate runtime binary for macOS arm64.

Most users should install this transitively:

```sh
npm install @substrate/sdk
```

The package exposes the runtime binary at `bin/substrate-runtime`.
